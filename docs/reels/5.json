[
  {
    "type": "concept",
    "heading": "3-Sum = Sort + Two Pointers",
    "body": "Fix one number `nums[i]`, then find **two numbers** in the rest that sum to `-nums[i]`.\n\n- **Sort first** so you can use two pointers\n- **Skip duplicates** at both the outer loop and inner pointers to ensure unique triplets\n- Time: **O(n²)** — outer loop × inner two-pointer scan"
  },
  {
    "type": "concept",
    "heading": "The Duplicate-Skipping Dance",
    "body": "**Outer loop:** `if i > 0 and nums[i] == nums[i-1]: continue`\n\n**After finding a triplet:**\n- Skip `left` forward while `nums[left] == nums[left+1]`\n- Skip `right` backward while `nums[right] == nums[right-1]`\n- Then move both pointers one more step\n\nWithout this, you'll get duplicate triplets."
  },
  {
    "type": "code",
    "label": "Crux · Python",
    "code": "nums.sort()\nfor i in range(len(nums) - 2):\n    if i > 0 and nums[i] == nums[i - 1]:\n        continue\n    left, right = i + 1, len(nums) - 1\n    while left < right:\n        total = nums[i] + nums[left] + nums[right]\n        if total < 0:\n            left += 1\n        elif total > 0:\n            right -= 1\n        else:\n            result.append([nums[i], nums[left], nums[right]])\n            while left < right and nums[left] == nums[left + 1]:\n                left += 1\n            while left < right and nums[right] == nums[right - 1]:\n                right -= 1\n            left += 1\n            right -= 1"
  },
  {
    "type": "quiz",
    "q": "What is the time complexity of the 3-Sum solution?",
    "options": [
      "O(n log n)",
      "O(n²)",
      "O(n³)",
      "O(n)"
    ],
    "answer": 1,
    "explain": "Sorting is O(n log n), but the nested loop (outer × inner two-pointer) dominates at O(n²)."
  }
]
